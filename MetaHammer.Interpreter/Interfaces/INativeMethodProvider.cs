/*
namespace MetaHammer.Interpreter.Interfaces;

/// <summary>
/// Define el catálogo de funciones nativas (implementadas en C#)
/// disponibles para el motor de ejecución.
/// </summary>
public interface INativeMethodProvider
{
    /// <summary>
    /// Obtiene una implementación nativa específica mediante su identificador único.
    /// </summary>
    /// <param name="identifier">El identificador de la función (ej: "system.math.calculate").</param>
    /// <returns>La implementación de la función nativa.</returns>
    /// <exception cref="UnresolvedNativeFunctionException">Si el identificador no está registrado.</exception>
    INativeMethod Get(string identifier);

    /// <summary>
    /// Verifica si una función nativa está registrada.
    /// </summary>
    bool Exists(string identifier);

    /// <summary>
    /// Retorna todas las funciones registradas (útil para la fase de diseño/UI).
    /// </summary>
    IEnumerable<INativeMethod> GetAll();
}
*/
